{
  "name": "@johnrobinsn/claude-watch",
  "version": "0.6.0",
  "description": "TUI dashboard for monitoring Claude Code sessions with tmux integration",
  "type": "module",
  "main": "dist/cli.js",
  "bin": {
    "claude-watch": "dist/cli.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/johnrobinsn/claude-watch.git"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "dev:serve": "npm run build && node --watch dist/cli.js serve",
    "start": "node dist/cli.js",
    "start:serve": "node dist/cli.js serve",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src tests --ext .ts,.tsx",
    "format": "prettier --write 'src/**/*.{ts,tsx}' 'tests/**/*.{ts,tsx}'",
    "prepublishOnly": "npm run build",
    "preuninstall": "claude-watch --cleanup || true",
    "version": "node -e \"const v=require('./package.json').version; require('fs').writeFileSync('src/utils/version.ts', '/**\\n * Central version constant for claude-watch.\\n * Keep in sync with package.json version.\\n */\\nexport const VERSION = \\\"'+v+'\\\";\\n')\" && git add src/utils/version.ts"
  },
  "keywords": [
    "claude",
    "claude-code",
    "tui",
    "tmux",
    "monitoring",
    "dashboard"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "hono": "^4.6.0",
    "ink": "^5.1.0",
    "react": "^18.3.1"
  },
  "devDependencies": {
    "@hono/node-server": "^1.13.0",
    "@types/bun": "^1.1.0",
    "@types/node": "^22.10.0",
    "@types/react": "^18.3.12",
    "@typescript-eslint/eslint-plugin": "^8.18.0",
    "@typescript-eslint/parser": "^8.18.0",
    "@vitest/coverage-istanbul": "^2.1.8",
    "eslint": "^8.57.0",
    "ink-testing-library": "^4.0.0",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
